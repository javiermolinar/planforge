function parseAndSum(input) {
  // TODO(planforge-e2e): implement parser for comma-separated signed integers.
  // Expected behavior lives in test/calc.test.js.
  return 0;
}

module.exports = {
  parseAndSum,
};
